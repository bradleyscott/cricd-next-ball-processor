{
  "name": "cricd-next-ball-processor",
  "version": "1.0.0",
  "description": "An HTTP API that can tell you the batsmen, bowler, innings, over and ball for the expected next delivery",
  "main": "index.js",
  "scripts": {
    "debug": "DEBUG=* EVENTSTORE_HOST=localhost ENTITIES_HOST=localhost node --inspect index.js",
    "debug-windows": "SET DEBUG=*&& SET EVENTSTORE_HOST=localhost&& SET ENTITIES_host=localhost&& node --inspect index.js",
    "start": "DEBUG=* node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cricd/next-ball-processor.git"
  },
  "author": "Bradley Scott",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cricd/next-ball-processor/issues"
  },
  "homepage": "https://github.com/cricd/next-ball-processor#readme",
  "dependencies": {
    "bluebird": "^3.4.1",
    "cors": "^2.8.1",
    "debug": "^2.2.0",
    "express": "^4.14.0",
    "ges-client": "^0.10.0",
    "node-rest-client": "^2.0.1",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "mocha": "^3.0.2",
    "should": "^11.1.0"
  }
}
